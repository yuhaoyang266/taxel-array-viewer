# 数据接入说明 — tri_axis_array 可视化前端

> 面向传感器/采集侧的接入契约。回答一个问题：**前端支持什么格式的数据，传感器怎么把数据传进去。**
> 对应代码：本仓库根目录；力矩聚合依赖包含在 `tri_axis_array/code/estimate.py`。

## 一句话结论

前端只认一种数据：**标定后的三轴力数组，单位牛顿**。传感器侧把每帧的
fx/fy/fz 换算成 N，交给一个 Python 函数，界面零改动。

## 1. 帧契约（前端消费的最小单位）

**阵列模式**（`pyqt_live.py` / `rerun_live.py`），每帧：

```text
readings : numpy float64, shape (rows, cols, 3)
readings[row, col] = [fx, fy, fz]   # 单位 N，必须有限（不能 NaN/inf）
```

**单元素模式**（`pyqt_single_taxel.py`），每帧：

```text
force : shape (3,) = [fx, fy, fz]   # 单位 N，必须有限
```

**一次性几何配置**（启动时给定，与数据帧分离）：

```text
rows, cols                     # 阵列行列数
pitch_x_mm, pitch_y_mm         # 元素中心间距
taxel_width_x_mm, taxel_width_y_mm  # 元件尺寸
hz                             # 采样/刷新率
```

## 2. 坐标系与单位约定

| 项 | 约定 |
|---|---|
| +x | 屏幕向右（列方向） |
| +y | 屏幕向上（行方向） |
| +z | 出传感器面（法向） |
| 力 | N |
| 位置 | 内部 m，界面 mm |
| 力矩 | N·m，绕阵列几何中心，右手系 |

## 3. 前端假设的测量模型

每个传感元件**只测三轴力、不测局部扭矩**；元件位置由间距几何推出。
合力/合力矩按已验证的聚合律计算：

```text
F = Σ_i f_i
M = Σ_i (r_i − r_0) × f_i      # r_0 = 阵列几何中心
```

## 4. 传感器侧需要提供什么（二选一）

**方案 A（推荐，边界最干净）：直接给标定后的牛顿值**

1. 每帧：时间戳 + 每元件 `[fx, fy, fz]`（N）
2. 元件→`(row, col)` 的排布映射，以及元件安装朝向（有无旋转——
   旋转安装的元件其局部 fx/fy 需先转到阵列坐标系）
3. 采样率

**方案 B：给原始值（电容/counts）+ 标定曲线，我们这边反演**

1. 原始帧：每元件三通道原始值 + 时间戳 + 采集有效位
2. 每元件 × 每通道的**力-电容标定曲线**：
   - 采样点表 + 声明的插值方法（或参数式系数）
   - 拟合方向（标定是 加力→测电容 F→C；运行时反着用 C→F，需曲线单调）
   - 有效域（电容窗口 + 力窗口，出域帧按无效处理，**不外推**）
   - 迟滞声明（加载/卸载曲线是否重合；不重合时偏差计入不确定度）
   - 无载基线（tare）与漂移处理策略

**硬边界**：原始 counts 不要直接当 N 灌进来——界面上所有力/力矩
数字、曲线、杠杆臂计算只在标定后成立。

## 5. 接入机制

前端是单个 Python 进程，接缝 = **一个函数**，每个刷新节拍调用一次：

```python
def next_readings(elapsed_s: float) -> np.ndarray:
    """返回 (rows, cols, 3) float64，单位 N。"""
    ...
```

把 `live_source.py` 里的合成生成器换成真数据版本即可，可视化代码不动。
传输层（串口/UDP/CAN/厂商 SDK 解析）在传感器侧完成，前端不管。

文件重放同样支持：把 `(t, 数组)` 序列录成 `.npz`，前端配一个重放
生成器即可离线回放。

## 6. 当前状态

| 入口 | 状态 | 说明 |
|---|---|---|
| `pyqt_live.py` | 可用，合成源 | 阵列实时视图：色块=fz、箭头=fx/fy、点击联动历史 |
| `pyqt_single_taxel.py` | 可用，合成源 | 单元件三通道历史视图（当前真数据=1 个元件的落点） |
| `rerun_live.py` | 可用，合成源 | 3D 箭头 + 六分量曲线，Rerun Viewer / `.rrd` 录制 |
| 力/力矩云图 | 可用，合成源 | `pyqt_live.py --view cloud`；通道 Fz、\|剪切\|、Mx/My/Mz，支持自动或固定色标；仅作显示插值 |

运行环境为隔离 conda 环境（`taxel-pyqt`：numpy/pyqtgraph/PySide6），
详见 [README.md](README.md)。
